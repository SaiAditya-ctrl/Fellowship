from django.urls import path
from django.conf.urls import url
from . import views
app_name='jwt'
urlpatterns=[
    path('',views.index,name='index'),
    path('registration/',views.registration,name='registration'),
    path('registration/saving/',views.saving,name='saving'),
    path('registration/login',views.login,name='login'),
    path('registration/func/',views.func,name='func'),
    path('registration/not_found',views.not_found,name='not_found'),













































































































































































    ]
